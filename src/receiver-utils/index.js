/**
 * emulator-devtool.js
 * - When running receiver out of Google Cast device
 * - When you need to record scenario that running on Google Cast device
 */

require('./device-polyfill');
require('./scenario-recorder');
